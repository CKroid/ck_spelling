name: ck_spelling
description: "CK Spelling"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  file_picker: ^10.3.10
  file_saver: ^0.3.1
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.5
  package_info_plus: ^8.0.0
  hive_flutter: ^1.1.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
